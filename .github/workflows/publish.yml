# No longer needed - serving from docs/ folder directly
# Delete this file: git rm .github/workflows/publish.yml
name: unused
on: workflow_dispatch
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "not used"
